{
  "name": "@promethean-os/api-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Unified backend facade for workbench consolidation",
  "license": "GPL-3.0-only",
  "scripts": {
    "dev": "REDACTED_SECRET --loader ts-REDACTED_SECRET/esm src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "REDACTED_SECRET dist/main.js",
    "test": "pnpm run build && REDACTED_SECRET --test dist/tests/*.test.js"
  },
  "dependencies": {
    "@fastify/sensible": "^5.6.0",
    "fastify": "^4.27.0"
  },
  "devDependencies": {
    "@types/REDACTED_SECRET": "^22.10.0",
    "ts-REDACTED_SECRET": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
