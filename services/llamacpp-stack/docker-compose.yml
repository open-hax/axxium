services:
  llamacpp-chat:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    restart: unless-stopped
    runtime: nvidia
    ports:
      - "127.0.0.1:${LLAMACPP_CHAT_PORT:-8080}:8080"
    environment:
      CUDA_VISIBLE_DEVICES: "${CUDA_VISIBLE_DEVICES:-0}"
      NVIDIA_VISIBLE_DEVICES: "${NVIDIA_VISIBLE_DEVICES:-all}"
      NVIDIA_DRIVER_CAPABILITIES: "${NVIDIA_DRIVER_CAPABILITIES:-compute,utility}"
      LLAMA_ARG_HOST: "0.0.0.0"
      LLAMA_ARG_PORT: "8080"
      LLAMA_ARG_N_GPU_LAYERS: "${LLAMACPP_N_GPU_LAYERS:-999}"
      LLAMA_ARG_CTX_SIZE: "${LLAMACPP_CTX_SIZE:-8192}"
      LLAMA_ARG_PARALLEL: "${LLAMACPP_PARALLEL:-4}"
      LLAMA_ARG_MODEL: "/models/${LLAMACPP_CHAT_MODEL:-gemma-4-e4b-Q4_K_M.gguf}"
    volumes:
      - ${LLAMACPP_MODELS_DIR:-./models}:/models:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8080/health"]
      interval: 20s
      timeout: 10s
      retries: 5
      start_period: 30s
    networks:
      ai-infra:
        aliases:
          - llamacpp-chat
    user: ${DOCKER_USER:-1000:1000}

  llamacpp-embed:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    restart: unless-stopped
    runtime: nvidia
    ports:
      - "127.0.0.1:${LLAMACPP_EMBED_PORT:-8081}:8081"
    environment:
      CUDA_VISIBLE_DEVICES: "${CUDA_VISIBLE_DEVICES:-0}"
      NVIDIA_VISIBLE_DEVICES: "${NVIDIA_VISIBLE_DEVICES:-all}"
      NVIDIA_DRIVER_CAPABILITIES: "${NVIDIA_DRIVER_CAPABILITIES:-compute,utility}"
      LLAMA_ARG_HOST: "0.0.0.0"
      LLAMA_ARG_PORT: "8081"
      LLAMA_ARG_N_GPU_LAYERS: "${LLAMACPP_N_GPU_LAYERS:-999}"
      LLAMA_ARG_EMBEDDING: "true"
      LLAMA_ARG_MODEL: "/models/${LLAMACPP_EMBED_MODEL:-qwen3-embedding-0.6b-F16.gguf}"
    volumes:
      - ${LLAMACPP_MODELS_DIR:-./models}:/models:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8081/health"]
      interval: 20s
      timeout: 10s
      retries: 5
      start_period: 20s
    networks:
      ai-infra:
        aliases:
          - llamacpp-embed
    user: ${DOCKER_USER:-1000:1000}

networks:
  ai-infra:
    external: true
    name: ai-infra
